name: flutter_sample
description: flutter riverpod architecture sample.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  hooks_riverpod: ^0.13.1
  flutter_hooks: ^0.16.0
  state_notifier: ^0.7.0
  freezed_annotation:

  #Firebase
  firebase_core: ^1.0.1
  firebase_auth: ^1.0.1
  cloud_firestore: ^1.0.1

  gap: ^2.0.0-nullsafety.1
  flutter_reorderable_list: ^1.0.0

  simple_logger: ^1.7.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  freezed:
  json_serializable:

flutter:

  uses-material-design: true
